name: 🔁 Auto Update Toffee Playlist

on:
  schedule:
    - cron: "*/5 * * * *"   # ⏰ প্রতি ৫ মিনিটে অটো আপডেট
  workflow_dispatch:        # 🧑‍💻 ম্যানুয়ালি চালানো যাবে

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📥 Install dependencies
        run: pip install requests

      - name: 🔄 Download & Update Toffee Playlist
        run: python auto_toffee_playlist.py

      - name: 💾 Commit & Push updates
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add Toffee.m3u update_log.txt
          git commit -m "🔁 Auto update Toffee.m3u [$(date '+%Y-%m-%d %H:%M:%S')]" || echo "No changes to commit"
          git push
